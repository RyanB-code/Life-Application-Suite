{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "C/C++: g++.exe build active file",
            "command": "C:\\msys64\\mingw64\\bin\\g++.exe",
            "args": [
                "-fdiagnostics-color=always",
                "-g",
                "-w",                                               // Disable warnings
                "${workspaceFolder}/include/DearImGUI/*.cpp",       // Build cpp files in DearImGUI diectory
                "${workspaceFolder}/src/Modules/*.cpp",             // Build cpp files in Modules directory
                "${workspaceFolder}/src/*.cpp",                     // Build cpp files in the current workspace folder

                "-I${workspaceFolder}/include",                     // Specifies include folder for g++
		       "-L${workspaceFolder}/lib",                          // Specifies include lib path for g++
                
                "-o",
                "C:\\dev\\1. Project Development Files\\LAS\\LAS-v0.2.0-WIP.exe",   // Name the .exe and put to file
                "-std=c++20",                                                       // Tells g++ to use C++20

                "-lImGui",                                          // Libraries specifed it needs to link against
                "-lglfw3dll", 
                "-lRST",
                "-lglad", 
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        }
    ],
    "version": "2.0.0"
}